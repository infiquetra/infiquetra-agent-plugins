{
  "source_repository": "https://github.com/infiquetra/infiquetra-claude-plugins",
  "source_commit": "3b5faa6c1044a888e03cb7b8bbf2f71c6749489c",
  "source_version": "0.25.2",
  "source_package_path": "plugins/fleet-core",
  "notes": [
    "Custody does not move. This package is a derived artifact, never a second writable source for retry_backoff. A retry fix lands in infiquetra-claude-plugins first and reaches this repository by re-synchronization. Fleet Core 0.25.1 was the first release to exercise that rule; 0.25.2 is the second. The non-finite Retry-After repair -- float() accepts inf, nan, and 1e400, so a header carrying one parsed to a non-finite delay that destroyed the caller's typed 429 surface at math.ceil -- was authored, reviewed, and released upstream, and arrived here as a re-synchronization rather than as an edit to these bytes.",
    "The pinned commit is the revision at which the upstream plugins/fleet-core subtree last changed. It is the head of the upstream default branch, and the subtree is byte-identical between the two, so the pin names the exact revision these bytes came from rather than an unrelated later head.",
    "The portable UniFi package pins c835f91d (unifi 2.0.2) while this slice pins 3b5faa6c (fleet-core 0.25.2). Both name points on one line of upstream history: 3b5faa6c is the revision at which the upstream plugins/fleet-core subtree last changed, and c835f91d is its direct descendant, which changed plugins/unifi and left the fleet-core subtree byte-identical. Two pins, one consistent upstream state, read at the two points where each subtree last moved. The plan's requirement R3, which forbids pinning 995a475, is satisfied by both.",
    "scripts/sync_vendor_source.py derives only plugins/unifi/; it carries no Fleet Core target. This slice's byte copy is therefore extracted from the pinned commit with `git show <commit>:<source_path>`, which is the same primitive that script uses internally to read a source byte, so the copy is a mechanical extraction from the pinned revision and never a hand edit. Generalizing the synchronization script to a second package is named follow-up work in docs/engineering-journal/QUEUED.md; until it lands, the digest equality recorded here and recomputed by scripts/check_repo.py is what proves the extraction was faithful.",
    "The module imports UTC from datetime, which exists only in Python 3.11 and newer. The catalog's floor is python>=3.12 by operator decision of 2026-08-22, recorded in docs/engineering-journal/DECISIONS.md with the superseded 3.10 rationale preserved, so this module sits inside the declared contract with room to spare.",
    "Paths in `files` are relative to this package root, which is what the repository validator resolves. Paths in `derived_files` and `release_surface` are relative to the repository root, because the ported test lives outside the package tree and cannot be expressed as a package-relative path."
  ],
  "files": [
    {
      "path": "plugin.json",
      "classification": "target-owned"
    },
    {
      "path": "README.md",
      "classification": "target-owned"
    },
    {
      "path": "CHANGELOG.md",
      "classification": "target-owned"
    },
    {
      "path": "DEFERRED.md",
      "classification": "target-owned"
    },
    {
      "path": "scripts/fleet_commons/retry_backoff.py",
      "classification": "upstream-byte-copy",
      "source_path": "plugins/fleet-core/scripts/fleet_commons/retry_backoff.py",
      "sha256": "2aa7fd26bb0fb40dbbd0b7a14ae34f24c473561648695346edaa60079ac63021"
    },
    {
      "path": "scripts/fleet_commons/intent_envelope.py",
      "classification": "deterministic-transform",
      "source_path": "plugins/fleet-core/scripts/fleet_commons/intent_envelope.py",
      "source_sha256": "5157fa303a92152ec9af0ef2a8f8e7543a46351d84282e076854ac2a2f9d201a",
      "sha256": "c4e9b522c73e23a875ae85baa5e45687f4bfaae84fa3ead11af1d4c730d50b98",
      "transform": "resolve-fleet-commons-sibling",
      "transform_version": "1",
      "transform_rule": "Replace the upstream module-scope sys.path insert plus fleet-commons discovery-shim import block (lines 79-83 of the source) with a `_load_sibling(name)` helper that resolves `<name>.py` from this file's own directory, and replace the discovery shim's two `.load(...)` call sites for tier_resolver and tier_palette (lines 166 and 170 of the source) with `_load_sibling(\"tier_resolver\")` and `_load_sibling(\"tier_palette\")`. Add `import importlib.util` as the first standard-library import. Sibling resolution is placement-independent by construction: the identical transformed file works in plugins/fleet-core/scripts/fleet_commons/ and in any generated _bundled/ copy a consuming plugin carries, because a module is always loaded from the directory holding this file. `_load_sibling` mirrors the discovery shim's load contract: lazy, cached in sys.modules under a `_fleet_commons_<name>@<sibling directory>` key, and a name with no sibling file (the deferred tier_resolver) raises RuntimeError at call time naming the resolved missing sibling path, never at import. Every other line is copied byte for byte."
    },
    {
      "path": "scripts/fleet_commons/models.json",
      "classification": "upstream-byte-copy",
      "source_path": "plugins/fleet-core/scripts/fleet_commons/models.json",
      "sha256": "8b50e821e12d56d555e2c2f087df2f568fda48c20987840d2d5a3bfa7a362f83"
    },
    {
      "path": "scripts/fleet_commons/tier_palette.py",
      "classification": "upstream-byte-copy",
      "source_path": "plugins/fleet-core/scripts/fleet_commons/tier_palette.py",
      "sha256": "b14ff89f155c0043e72bf028b937bdb6e3e7b4ebbfbf919683d88a8764ef9e28"
    }
  ],
  "derived_files": [
    {
      "path": "tests/test_retry_backoff.py",
      "classification": "deterministic-transform",
      "source_path": "tests/test_retry_backoff.py",
      "source_sha256": "07ec46c506430548af957179aa245155d004d7bd9a8cff46ff2c598345575f68",
      "sha256": "eb1b7b486a064b68bc9e63e964f076bbb40952f77b1708d001d91b520273f9b1",
      "transform": "guard-pytest-import",
      "transform_version": "2",
      "transform_rule": "Replace the upstream module docstring with one recording the port, and replace the unconditional `import pytest` with a ModuleNotFoundError-guarded import whose absent-pytest branch raises `unittest.SkipTest`. Every line from `class RateError(Exception):` to end of file is copied byte for byte, so all eighteen upstream test functions are unchanged. The guard exists because this repository's hermetic baseline job runs `python3 -m unittest discover -s tests` with no dependencies installed and imports every test module it finds, while the ported plugin job runs `python -m pytest tests` with pytest installed. Version 2 replaced version 1's `pytest = None` fallback because Fleet Core 0.25.1 added two tests carrying `@pytest.mark.parametrize`: a decorator is evaluated when the module is imported, and against `None` it raises AttributeError, which would fail the baseline job on a module it never runs. `unittest` catches `SkipTest` raised during import and records the module as one skipped test, so the baseline stays green and names the reason out loud."
    }
  ],
  "release_surface": {
    "declared_by": "R33",
    "items": [
      "plugins/fleet-core/plugin.json",
      "plugins/fleet-core/PROVENANCE.json",
      "plugins/fleet-core/CHANGELOG.md",
      "plugins/fleet-core/scripts/fleet_commons/retry_backoff.py",
      "plugins/fleet-core/scripts/fleet_commons/intent_envelope.py",
      "plugins/fleet-core/scripts/fleet_commons/models.json",
      "plugins/fleet-core/scripts/fleet_commons/tier_palette.py",
      "tests/test_retry_backoff.py",
      "tests/test_intent_envelope.py",
      "tests/test_tier_palette.py"
    ],
    "package_documentation": [
      "plugins/fleet-core/README.md",
      "plugins/fleet-core/DEFERRED.md"
    ]
  }
}
