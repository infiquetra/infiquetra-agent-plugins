<!-- Generated by scripts/generate_deferred_inventory.py. Do not edit by hand. -->

# Deferred Fleet Core modules

This package ports one vertical slice of Fleet Core. Everything else the
upstream source carries is named below, so no reader has to infer what is
missing and no document has to claim full Fleet Core parity.

## Counting basis

Every path under the upstream `fleet_commons` package, plus the
`fleet_commons_shim.py` module beside it, split into Python modules and data
files, minus what this slice ports. The inventory is derived from the
pinned source tree by `scripts/generate_deferred_inventory.py` rather than
typed, and it is verified by set difference rather than against a literal
count, so a module added upstream fails the check until this file is
regenerated.

The shim module `fleet_commons_shim.py` appears in the inventory because it sits
beside the package upstream and belongs to the same surface. It is not
merely deferred: its plugin discovery is specific to Claude Code, so the
portable packaging model replaces it with a build-time bundle rather than
ever porting it.

## Pinned source

| Field | Value |
|---|---|
| Source repository | `https://github.com/infiquetra/infiquetra-claude-plugins` |
| Source commit | `ed72f439ba01f2e20d94be074e5612c5641c0c8e` |
| Fleet Core version | `0.25.1` |

Custody does not move. A future change to any module below lands upstream
first and reaches this repository by re-synchronization.

## Ported by this package

| Item | Kind |
|---|---|
| `fleet_commons/retry_backoff.py` | Python module |

## Deferred: present upstream, absent here

| Item | Kind |
|---|---|
| `fleet_commons/audit_store.py` | Python module |
| `fleet_commons/bridge_receipt.py` | Python module |
| `fleet_commons/concurrency_policy.py` | Python module |
| `fleet_commons/cost_weights.json` | data file |
| `fleet_commons/cost_weights.py` | Python module |
| `fleet_commons/delegation_audit.py` | Python module |
| `fleet_commons/delegation_state.py` | Python module |
| `fleet_commons/effort_rider.py` | Python module |
| `fleet_commons/intent_envelope.py` | Python module |
| `fleet_commons/liveness_engine.py` | Python module |
| `fleet_commons/models.json` | data file |
| `fleet_commons/output_attestation.py` | Python module |
| `fleet_commons/plugin_resolution.py` | Python module |
| `fleet_commons/render_tier_table.py` | Python module |
| `fleet_commons/tier_palette.py` | Python module |
| `fleet_commons/tier_policy.json` | data file |
| `fleet_commons/tier_resolver.py` | Python module |
| `fleet_commons_shim.py` | Python module |

## Counts

| Basis | Python modules | Data files | Total |
|---|---|---|---|
| Upstream inventory | 16 | 3 | 19 |
| Ported by this package | 1 | 0 | 1 |
| Deferred | 15 | 3 | 18 |
