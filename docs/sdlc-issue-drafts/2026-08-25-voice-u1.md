---
title: voice U1 — package foundation, provider declaration contract, and subprocess discipline
repo: infiquetra-agent-plugins
type: capability
team: asgard
project: operations
status: Shaping
labels: capability, needs-plan
risk: medium
handoff_maturity: requirements-ready
parent: infiquetra/infiquetra-agent-plugins#27
unit: U1
lane: G1
depends_on: none — this is the foundation lane
---

### Objective

Create the `voice` package skeleton and the contracts every other unit imports: the
provider declaration model, the closed egress-class set, the stated retention posture,
and the subprocess discipline helper.

This unit owns the portable package root. Every other unit depends on it and adds files
alongside it; no other unit edits the files listed below.

**This unit implements R30.** The README states plainly that the package carries no
provenance manifest and no port descriptor. U7 later verifies and preserves that truth
while finalizing the document, but does not re-own or re-implement it.

**Unit U1 · lane G1 · depends on none — this is the foundation lane.**
**Requirements owned: R20, R21, R23, R24, R28, R29, R30, R31, R32.** These requirements belong to this unit alone; no
other unit in the run implements them.

Parent contract: infiquetra/infiquetra-agent-plugins#27

### Intent

Establish the portable core so the remaining six units have a stable contract to build
against.

The provider contract is the load-bearing piece. `voice` ships no provider
implementations (R20, R24). The operator declares each provider with its invocation or
endpoint, its capabilities, its egress class, and the *name* of any credential
environment variable it needs — never the value. A provider that is unavailable
produces a named refusal; substitution is never silent (R23).

**Package shape follows this repository's established convention**, verified against
`plugins/unifi/` and `plugins/mission-control/`: portable core at `plugins/voice/`
with vendor-neutral `scripts/` and `skills/`, and the Claude client extension confined
to `plugins/voice/com.infiquetra.claude/` (U2 owns that directory). Hooks live in the
client extension, never in portable core (R29), per `AGENTS.md:52-53`.

**Both runtime providers are resolved, and their non-secret settings live here.** U1
owns the settings surface that U3 and U4 consume:

| Setting | Purpose | Version-one acceptance value |
| --- | --- | --- |
| `VOICE_FORGE_BASE_URL` | Voice Forge text-to-speech base URL on the Mac mini | From the Home Lab deployment receipt. **Never a hard-coded IP address** |
| `VOICE_FORGE_VOICE_ID` | Selects a registered Voice Forge voice | Runtime configuration, not an operator gate |
| `VOICE_HERMES_BASE_URL` | Local Hermes relay base URL | `http://127.0.0.1:8765` |
| `VOICE_HERMES_PROFILE` | Hermes profile scoping the transcription | `mimir-engineer` |

All four are **non-secret**. `voice` holds no credential of its own: Hermes owns the
xAI OAuth token and the xAI request. The one token `voice` touches is Hermes' rotating
loopback **session** token, read from the local dashboard root at call time and held in
process memory only — a transport detail owned by U4, never a declared credential, never
an environment variable, and never persisted or logged. The declaration contract's credential-variable-name
field is therefore **empty** for both providers — which the contract must permit, since
R20 requires the field to record a name rather than a value, and here there is no name
to record.

Egress classes differ and must both be stated (R21): Voice Forge is `local-network`;
the Hermes relay route is **`external`**, because Hermes forwards the audio on to xAI.
The relay being on loopback does not make the audio local.

Retention posture is settled by operator decision D5 and is not open in planning:
temporary audio is deleted after both success and failure, `voice` keeps no transcript
log of its own, and it emits no telemetry. Planning chooses the smallest clear setting
*name*; the behaviour itself is decided. Per R28 it is written down rather than left as
a silent default.

Every subprocess the package starts closes standard input explicitly and attaches a
deadline (R32). This is a safety requirement carried from the engineering journal.

**Proportionality.** `voice` is a private, single-user developer plugin — not a
Pentagon system. Prefer the smallest compatible implementation. Standard-library
Python at the repository floor of `>=3.12`, tested with `unittest` (R31). Do not add
enterprise, multi-tenant, internet-scale, high-availability, regulatory, or overly
defensive machinery without an explicit requirement or a demonstrated failure.

Do not relax real boundaries in the name of that smallness: credential handling, shell
execution, filesystem writes, Git operations, privacy and retention, destructive
actions, and production safety all keep their full strength.

**Board ownership.** The coordinator is the sole Operations-board writer. This unit
never writes board state; it produces commits, a pull request, and a typed review
outcome, and the coordinator moves the card.

### Out-of-scope / non-goals

- Any provider implementation, credential handling, billing, or service lifecycle.
- The Claude client extension at `plugins/voice/com.infiquetra.claude/` — U2 owns it.
- The Agent Skill entrypoint and its CLI — U6 owns those.
- Speech synthesis, recording, transcription, delivery, or pane UI.
- A Model Context Protocol server of any kind; version one excludes it.
- Editing `.github/workflows/ci.yml`; the existing `plugins/*/tests` glob already
  collects this package's tests.

**Shared-surface rule.** This unit writes only the files listed below. If it concludes
it must edit a file owned by another unit, it stops and raises the conflict on the
parent contract rather than editing across the boundary.

### Files expected to change

- `plugins/voice/plugin.json`
- `plugins/voice/README.md`
- `plugins/voice/scripts/providers.py`
- `plugins/voice/scripts/settings.py`
- `plugins/voice/scripts/process.py`
- `plugins/voice/tests/test_providers.py`
- `plugins/voice/tests/test_settings.py`
- `plugins/voice/tests/test_process.py`

### Tests to add or update

- `plugins/voice/tests/test_providers.py` — a declaration carries invocation or
  endpoint, capabilities, egress class, and a credential variable *name* with no value;
  an unknown egress class is rejected; an unavailable provider raises a named refusal
  rather than falling back.
- `plugins/voice/tests/test_settings.py` — the ephemeral retention posture is stated,
  not defaulted; an absent value is not silently treated as empty; the four non-secret
  provider settings resolve, and no setting carries a secret.
- `plugins/voice/tests/test_process.py` — every spawned subprocess has standard input
  closed and a deadline attached.

### Context library links

- parent contract: infiquetra/infiquetra-agent-plugins#27
- requirements: docs/brainstorms/2026-08-25-voice-plugin-requirements.md
- ideation (evidence only): docs/ideation/2026-08-25-voice-plugin-ideation.md
- accepted review: docs/reviews/2026-08-25-voice-plugin-requirements-doc-review.md
- freshness review: docs/reviews/2026-08-25-voice-plugin-requirements-post-integration-freshness-review.md

### Acceptance criteria

- [ ] The portable provider contract imports cleanly: `python3 -c "import sys; sys.path.insert(0,'plugins/voice/scripts'); import providers"` → exits 0.
- [ ] Egress class is a closed set of exactly the four stated values: `grep -cE "on-device|local-network|named-remote-service|unofficial-remote-endpoint" plugins/voice/scripts/providers.py` → returns at least 4.
- [ ] The four non-secret provider settings are defined: `grep -cE "VOICE_FORGE_BASE_URL|VOICE_FORGE_VOICE_ID|VOICE_HERMES_BASE_URL|VOICE_HERMES_PROFILE" plugins/voice/scripts/settings.py` → returns at least 4.
- [ ] A declaration with no credential variable is valid, and no credential is read: `grep -riE "auth\.json|XAI_API_KEY" plugins/voice/scripts/` → returns no output.
- [ ] The README states the absent manifest and descriptor plainly (R30): `grep -iE "provenance|port descriptor" plugins/voice/README.md` → returns a match.
- [ ] Portable core contains no Claude hook (R29): `find plugins/voice/scripts -name '*hook*'` → returns no output.
- [ ] Repository validation passes with the new package present: `python3 scripts/check_repo.py` → prints `Repository validation passed.`.

### Verification

Run from the repository root at this unit's final commit:

```bash
python3 scripts/check_repo.py
python3 -m unittest discover -s tests
python3 -m pytest plugins/voice/tests -q
git diff --check
```

Expected: validation passes, the suite reports `OK`, the package tests report no
failures, and `git diff --check` is silent.

### Handoff maturity

requirements-ready

### Suggested next action

Covered by the run-wide `/plan` for issue #27. Do not plan this unit in isolation.

### Source context

- Source: docs/brainstorms/2026-08-25-voice-plugin-requirements.md
- Source type: brainstorm
- Source title: Voice Plugin — Requirements
- Source pin: `main` at `794fe46`
